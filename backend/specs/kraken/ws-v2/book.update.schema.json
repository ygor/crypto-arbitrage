{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.local/kraken/ws-v2/book.update.schema.json",
  "title": "Kraken WS v2 Book Update",
  "type": "object",
  "required": ["channel", "type", "data"],
  "properties": {
    "channel": { "pattern": "^book(-\\d+)?$" },
    "type": { "const": "update" },
    "data": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["symbol"],
        "properties": {
          "symbol": { "type": "string" },
          "bids": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["event", "limit_price", "order_qty"],
              "properties": {
                "event": { "enum": ["add", "modify", "delete"] },
                "limit_price": { "type": "number" },
                "order_qty": { "type": "number" }
              },
              "additionalProperties": true
            }
          },
          "asks": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["event", "limit_price", "order_qty"],
              "properties": {
                "event": { "enum": ["add", "modify", "delete"] },
                "limit_price": { "type": "number" },
                "order_qty": { "type": "number" }
              },
              "additionalProperties": true
            }
          }
        },
        "additionalProperties": true
      }
    }
  },
  "additionalProperties": true
} 